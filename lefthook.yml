# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#

commit-msg:
  commands:
    commitlint:
      run: pnpm dlx commitlint --edit $1

pre-commit:
  commands:
    biome-check:
      run: pnpm check {staged_files}
      stage_fixed: true
      exclude:
        - "*.{yml,yaml,md,toml,sql,html,png}"
        - ".gitignore"
        - "**/.env.*"
    ruff:
      glob: "apps/orchestrator/**/*.py"
      root: "apps/orchestrator/"
      run: uv run ruff format {staged_files} && uv run ruff check --fix {staged_files}
      stage_fixed: true
